name: Release

on:
  push:
    tags:
    - '*'

jobs:
  Release-Test:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Check out code
      - uses: actions/checkout@v2
      - run: echo "testing!"